Service-Based Architecture Style

if running on an arm system, add "platform: linux/amd64" to the shop_db service so the container's x86_64 intructions can be translated to the host machine's arm system.
